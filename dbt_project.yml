name: 'job_market_intelligence'
version: '1.0.0'
config-version: 2

profile: 'job_market'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  job_market_intelligence:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
